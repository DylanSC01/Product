import type { ProductNew } from "@/types/types";

interface ProductNewCardProps {
    product: ProductNew;
}

export const ProductNewCard = ({ product }: ProductNewCardProps) => {
  return (
    <div className="">ProductNewCard</div>
  )
}
